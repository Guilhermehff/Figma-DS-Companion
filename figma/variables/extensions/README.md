# Extension Exports

This directory is reserved for on-demand local exports of Figma semantic extension data.

Do not treat files here as live governance state.

Create extension snapshots only when a local audit or compatibility export is explicitly requested, then archive or remove them when that task is complete.
